/**
 * ЕДИНСТВЕННОЕ МЕСТО, ГДЕ ЖИВУТ ЧИСЛА, ОПРЕДЕЛЯЮЩИЕ ОЩУЩЕНИЕ ИГРЫ.
 *
 * Правило проекта: ни одной физической/временной константы за пределами этого файла.
 * Хочешь «прыжок повыше» или «дэш подлиннее» — правится здесь одной строкой.
 *
 * Единицы: пиксели и секунды. Мир построен на тайлах 16px.
 */

// ---------------------------------------------------------------- мир и экран

export const TILE = 16;
export const ROOM_W = 40; // тайлов
export const ROOM_H = 23; // тайлов
export const VIEW_W = ROOM_W * TILE; // 640
export const VIEW_H = ROOM_H * TILE; // 368

// ------------------------------------------------------------------- габариты

export const PLAYER_W = 12;
export const PLAYER_H = 18;

// ------------------------------------------------------- пропорции и шаг героя

/**
 * Скелет героя в пикселях, отсчёт от линии стоп вверх (значения отрицательные).
 * Рост укладывается в хитбокс PLAYER_H, кроме головы: она чуть выступает,
 * иначе на 18 пикселях фигура читается как обрубок.
 */
export const HERO_HIP_Y = -9.2;
export const HERO_CHEST_Y = -14.6;
export const HERO_SHOULDER_Y = -14.2;
export const HERO_HEAD_Y = -18.0;
export const HERO_HEAD_R = 2.0;

export const HERO_THIGH = 4.6;
export const HERO_SHIN = 4.7;
export const HERO_UPPER_ARM = 3.3;
export const HERO_FOREARM = 3.5;
/** На сколько вытягивается рука с фонарём от плеча. */
export const HERO_ARM_REACH = 6.2;

/**
 * Полуширина корпуса у таза и у плеч. Разница между ними и делает силуэт
 * человеческим: линия постоянной толщины читается как мешок, а не как торс.
 */
export const HERO_WAIST_W = 1.45;
export const HERO_CHEST_W = 2.15;
/** Длина ступни. Без неё бегущая фигура остаётся палочным человечком. */
export const HERO_FOOT = 2.4;
/** Длина шага. Больше — размашистее и «тяжелее» бег. */
export const HERO_STRIDE = 9.0;
/** На сколько стопа поднимается в маховой фазе. */
export const HERO_STEP_LIFT = 3.6;
/**
 * Циклов шага в секунду на единицу скорости и потолок частоты.
 * Анатомически честная частота на 180 px/s превратилась бы в мельтешение,
 * поэтому шаг намеренно «длиннее» скорости — так читается каждый шаг.
 */
export const HERO_WALK_SCALE = 0.026;
export const HERO_MAX_CYCLES = 4.6;
/** Подъём таза в опорной фазе — без него бег выглядит как езда на коньках. */
export const HERO_BOB = 1.0;
/** Наклон корпуса вперёд на полной скорости, радианы. */
export const HERO_LEAN = 0.24;

// ------------------------------------------------------------- бег по земле/в воздухе

/** Крейсерская скорость бега. */
export const MAX_RUN = 180;
/** Насколько быстро разгоняемся до MAX_RUN. */
export const RUN_ACCEL = 2000;
/** Насколько быстро гасится скорость СВЕРХ MAX_RUN (после дэша/вол-джампа). */
export const RUN_REDUCE = 800;
/** Множитель управляемости в воздухе. <1 — воздух «тяжелее» земли. */
export const AIR_MULT = 0.65;

// ------------------------------------------------------------------ гравитация

export const GRAVITY = 1800;
export const MAX_FALL = 320;
/** Скорость падения при зажатом «вниз» — быстрый спуск. */
export const FAST_MAX_FALL = 520;
/** У вершины прыжка (|vy| меньше порога) гравитация вдвое слабее — «зависание». */
export const HALF_GRAV_THRESHOLD = 80;

// --------------------------------------------------------------------- прыжок

export const JUMP_SPEED = 210;
/** Сколько секунд удержание кнопки продолжает тянуть вверх (переменная высота). */
export const VAR_JUMP_TIME = 0.2;
/** Горизонтальный «подхват» при прыжке в сторону движения. */
export const JUMP_H_BOOST = 80;
/** Прыжок ещё засчитывается столько секунд после того, как сошёл с края. */
export const COYOTE_TIME = 0.1;
/** Нажатие прыжка запоминается на столько секунд до приземления. */
export const JUMP_BUFFER = 0.12;

// ------------------------------------------------------------------ стены

/** Скорость сползания по стене. */
export const WALL_SLIDE_SPEED = 100;
/** Горизонтальный импульс отскока от стены. */
export const WALL_JUMP_H_SPEED = MAX_RUN + JUMP_H_BOOST; // 260
/** Сколько секунд после вол-джампа управление в воздухе ослаблено (иначе отскок «съедается»). */
export const WALL_JUMP_LOCK = 0.16;
/** На сколько пикселей ищем стену вбок — даёт «магнит» к стене. */
export const WALL_GRAB_REACH = 3;
/** Вол-джамп засчитывается столько секунд после отрыва от стены. */
export const WALL_COYOTE_TIME = 0.1;

// ---------------------------------------------------------------------- дэш

export const DASH_SPEED = 480;
export const DASH_TIME = 0.15;
/** Скорость, в которую переходим по окончании дэша. */
export const DASH_END_SPEED = 320;
/** Пауза перед следующим дэшем. */
export const DASH_COOLDOWN = 0.2;
/** Сколько дэшей даётся при касании земли. */
export const DASH_REFILL = 1;
/** Заморозка кадра в момент старта дэша — главный источник «сочности». */
export const DASH_FREEZE = 0.05;
/** Нажатие дэша запоминается на столько секунд. */
export const DASH_BUFFER = 0.1;
/** Секунды до респавна кристалла дэша после подбора. */
export const CRYSTAL_RESPAWN = 2.0;

// -------------------------------------------------------------------- смерть

export const DEATH_FREEZE = 0.06;
export const DEATH_TIME = 0.45;
export const RESPAWN_TIME = 0.25;

// ---------------------------------------------------------------------- juice

export const SHAKE_JUMP = 0;
export const SHAKE_LAND = 1.4;
export const SHAKE_DASH = 2.6;
export const SHAKE_DEATH = 7;
export const SHAKE_DECAY = 12; // насколько быстро гаснет тряска

/** Насколько сильно спрайт растягивается по вертикали в прыжке / сплющивается при посадке. */
export const SQUASH_JUMP = 0.34;
export const SQUASH_LAND = 0.38;
export const SQUASH_RECOVER = 9; // скорость возврата к нормальной форме

/**
 * Смещение камеры в сторону взгляда. Сейчас 0: комната ровно равна экрану,
 * поэтому любое смещение обнажает край карты. Включать вместе с картами
 * шире экрана (тогда же понадобится и CAM_SMOOTH).
 */
export const CAM_LOOKAHEAD = 0;
export const CAM_SMOOTH = 4;

// ----------------------------------------------------------------------- свет

/** Дальность луча фонаря. */
export const LIGHT_CONE_LEN = 210;
/** Половина угла раствора луча, радианы. */
export const LIGHT_CONE_HALF = 0.42;
/** Радиус «под ногами»: игрок никогда не бывает слеп вплотную к себе. */
export const LIGHT_AMBIENT_R = 52;
/** Насколько луч отклоняется вверх/вниз при нажатии ↑/↓. */
export const LIGHT_AIM_Y = 0.75;
/** Скорость доводки луча к целевому направлению — фонарь в руке, а не на турели. */
export const LIGHT_TURN = 14;
/**
 * Радиус света натриевой лампы. Держать МАЛЫМ: комната всего 640 px шириной,
 * и лампа радиусом в треть экрана заливает светом всё, а лампы отгоняют тварей.
 * При 200 px три лампы делали любую комнату безопасной, и враги стояли столбом.
 * Лампа — освещённый остров, а не прожектор.
 */
export const LAMP_R = 104;
/**
 * Насколько ярко видна геометрия ВНЕ света. Ноль — полная слепота.
 * Ниже ~0.35 платформер перестаёт быть честным: игрок перестаёт видеть, куда прыгает.
 */
export const DARK_VISIBILITY = 0.42;

// -------------------------------------------------------------------- радиация

/** Секунд в «фоне» до смерти. */
export const RAD_DEATH_TIME = 2.6;
/** Во сколько раз быстрее выветривается, чем набирается. */
export const RAD_RECOVER_MULT = 0.55;

// ----------------------------------------------------------------------- твари

/** Скорость подкрадывания в темноте. */
export const CREATURE_SPEED = 84;
/** Скорость отступления в луче — заметно выше: свет должен ощущаться оружием. */
export const CREATURE_FLEE_SPEED = 150;
/** С какого расстояния тварь чует игрока. */
export const CREATURE_AGGRO = 260;
/** Радиус смертельного касания. */
export const CREATURE_TOUCH = 11;
/** Насколько далеко тварь отходит от своего логова. */
export const CREATURE_LEASH = 150;

/**
 * Три вида тварей отличаются не только силуэтом, но и повадкой — иначе разный
 * рисунок остаётся декорацией. Множители применяются к базовым числам выше.
 *
 *  Упырь   — эталон: то, к чему привыкаешь в первых перегонах.
 *  Горбун  — медленный и тяжёлый, но с длинным замахом: близко не подойти.
 *  Нетопырь— быстрый и вертлявый, зато труслив: луч отгоняет его мгновенно.
 */
export const CREATURE_KINDS = {
  ghoul: { speed: 1.0, flee: 1.0, aggro: 1.0, touch: 1.0, leash: 1.0, wobble: 0.6, wobbleHz: 3.1 },
  brute: { speed: 0.62, flee: 0.55, aggro: 0.85, touch: 1.5, leash: 0.75, wobble: 0.25, wobbleHz: 1.6 },
  bat: { speed: 1.5, flee: 1.9, aggro: 1.15, touch: 0.75, leash: 1.5, wobble: 2.4, wobbleHz: 5.2 },
} as const;
export type CreatureKind = keyof typeof CREATURE_KINDS;

// -------------------------------------------------------- дрезины и гермозатворы

export const DRESINA_SPEED = 64;
/** Ширина дрезины в тайлах. */
export const DRESINA_TILES = 3;

/** Полный цикл гермозатвора: открыт → предупреждение → закрыт. */
export const DOOR_OPEN_TIME = 2.4;
export const DOOR_WARN_TIME = 0.7;
export const DOOR_CLOSED_TIME = 1.8;

// ------------------------------------------------------- обвал и тяга в тоннеле

/** Сколько шпала держится после того, как на неё наступили. */
export const CRUMBLE_DELAY = 0.32;
/** Через сколько секунд шпала восстанавливается. */
export const CRUMBLE_RESPAWN = 2.0;

/** Ускорение сквозняка. Задаётся на комнату (RoomDef.wind), здесь — множитель на земле. */
export const WIND_GROUND_MULT = 0.35;

// -------------------------------------------------------------- постобработка

/** Сила аддитивного свечения. 0 — выключить весь блум. */
export const POST_BLOOM = 0.32;
/** Радиус размытия копии кадра, пиксели устройства. */
export const POST_BLOOM_BLUR = 5;
/** Плотность плёночного зерна. Выше ~0.09 картинка начинает «шипеть». */
export const POST_GRAIN = 0.05;

// --------------------------------------------------------------------- физика шага

/** Максимальный фиксированный шаг симуляции. 1/120 — стабильно и детерминированно. */
export const FIXED_DT = 1 / 120;
export const MAX_STEPS_PER_FRAME = 8;
