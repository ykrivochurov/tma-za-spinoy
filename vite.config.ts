import { defineConfig } from 'vite';

export default defineConfig({
  /**
   * Относительная база — обязательна для GitHub Pages.
   *
   * Проектный сайт живёт не в корне домена, а по адресу вида
   * `https://<логин>.github.io/<репозиторий>/`. При базе по умолчанию (`/`)
   * сборка ссылается на `/assets/...`, браузер идёт в корень домена и получает 404:
   * страница открывается чёрной и пустой.
   *
   * `./` делает ссылки относительными, поэтому сборка работает в любом
   * подкаталоге — и на Pages, и локально при открытии файла напрямую.
   */
  base: './',
  build: {
    target: 'es2020',
    // Игра целиком в одном файле на ~63 КБ; дробить не на что.
    assetsInlineLimit: 0,
  },
});
